dataset:
  name: cpunish
  funct: cpunishDataset.cpunish_data
  batch_size: 14
optimizer:
  name: adam
  funct: torch.optim.Adam
  lr: 0.001
  beta_1: 0.5
  beta_2: 0.999
model:
  hidden_nodes: 10
train:
  criterion: torch.nn.BCELoss
  n_epochs: 1000
  initialize_generator: true
  warmUp_discriminator: true
  cuda: true
